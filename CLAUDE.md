# CLAUDE.md — London 2026 Trip Site

*Working reference for a fresh Claude Code session. Captures decisions and open threads from planning conversations so context isn't lost between sessions.*

---

## 1. Project Identity

A post-trip website for the family/friends UK trip: London as home base, with day trips to the Cotswolds and Cambridge. Repurposes an existing pre-trip itinerary HTML (originally built in a separate Claude conversation, hosted at a `claude.ai/public/artifacts/...` link) into a post-trip site with a map and photo galleries.

Trip dates:
- Left DC: 2026-06-28
- Landed Heathrow: 2026-06-29
- Left UK: 2026-07-07
- Separate: DC "reunion" dinner at Rania restaurant, 2026-07-10 (mom stayed behind in DC — on the site since 2026-07-13 as the "Dinner in DC at Rania" day block `day-10-jul` with its 11 photos (renamed from "Postscript — DC Reunion" same day: user considers it part of the same trip, not an addendum); deliberately NO map pin, a DC coordinate would wreck the UK map, #5 closed)

**Public-facing names are pseudonyms:** the site displays "Hugh" and "Betty" (no surname) instead of the real names, since the repo is public. Real names only exist in the gitignored `original/itinerary_source.html`. Don't reintroduce real names to `index.html`.

Audience: family and friends. Optimize for desktop, iPad, and phone — review on all three before calling the project done.

---

## 2. Repo Layout

```
London-2026/
├── index.html               # live site: hero (theme picker + 5-photo banner), tabbed Itinerary+Map / Photo Gallery, Leaflet map, lightbox gallery. Four CSS-variable themes via [data-theme] on <html>, dark is default (#47 PR #81, default flipped back in PR #82)
├── original/                 # GITIGNORED — preserved, unredacted source material. Never for the public repo.
│   └── itinerary_source.html # verbatim copy of the original pre-trip itinerary HTML, never modified except on explicit user direction
├── scripts/
│   ├── extract_trip_photos.py   # pulls EXIF date/GPS from a source folder, sorts into photos_raw/ (see section 5 — source is currently the USB import, not iCloud)
│   ├── suggest_photo_locations.py  # fills suggested_location in the manifest (nearest same-day flag within 600m); never touches final_location
│   ├── photo_enhance.py         # the single deterministic enhancement recipe (v2, skin-friendly) shared by review + build
│   ├── make_photo_review.py     # COMBINED review page (PR #78, was make_edit_review.py): location dropdown + quality before/after per photo, mine/mom's filter (see section 5)
│   ├── apply_photo_review.py    # applies the exported decisions JSON (was apply_photo_edits.py): quality → photo_edits.csv, locations → manifest final_location
│   ├── make_featured_picker.py  # #47 (PR #81): HTML tool to assign banner/day/item photo "slots" (see section 4) — read-only against the live gallery data already in index.html. #84 (PR #85): each slot preview is now a drag-to-reposition crop frame, not a plain thumbnail
│   ├── apply_featured_photos.py # applies the picker's export into data/featured_photos.csv (full rewrite, not merge — see section 4); also writes crop_x/crop_y since #84
│   └── make_contact_sheet.py    # one-page HTML contact sheet of photos_raw/pruned_review/ for the #24 manual pass
│   └── build_gallery.py         # converts photos_raw/uk_trip into web-ready JPGs in photos/, writes photos/manifest.json, and injects the data inline into index.html — incremental + parallel since #54; also resolves + injects the #47 banner/day/item photos
├── photos_raw/               # GITIGNORED — raw, uncurated photo dump sorted by date. Not for the public repo.
│   ├── iphone/               # current source: manual USB import (see section 5). Raw dump only — never delete files here directly, it won't be tracked (see section 5)
│   ├── uk_trip/               # matched trip photos, copied from whichever source is active. THIS is where the user deletes to curate — see section 5
│   ├── dc_reunion/
│   ├── moms_phone/           # mom's WhatsApp-export photos — a third curated folder since PR #78 (adopt/delete-track/un-delete like uk_trip); 109 unique photos + 2 parked MP4s (#31)
│   ├── moms_phone_dupes/     # exact byte-duplicates (WhatsApp double-downloads) moved aside by extract_trip_photos.py — 25 files, kept not deleted
│   └── pruned_review/        # files present in an older source but missing from the current one — moved here, not deleted, pending manual review (#24)
├── photos/                   # TRACKED — web-ready thumbs/full JPGs actually shipped on the site, organized by day-slug folder (e.g. day-29-jun/thumbs/, day-29-jun/full/), plus manifest.json
├── data/
│   ├── photo_manifest.csv    # GITIGNORED — filename, category, datetime, lat, lon, suggested_location, final_location, deleted, source (source: blank = user's phone, "moms_phone")
│   ├── flag_locations.csv    # TRACKED — small, public-place lookup (location_id, name, lat, lng, day) used to position map pins; Claude owns lat/lng, user only edits names
│   ├── photo_edits.csv       # TRACKED — filename,decision; build_gallery applies enhance() to approved rows at encode time (all 717 approved as of 2026-07-13)
│   └── featured_photos.csv   # TRACKED — role,filename,day,location_id,sort,crop_x,crop_y (#47, crop_x/crop_y added #84); "filename" holds a SLUG (e.g. "img_2830"), not the raw manifest filename — see section 4. Edit via the picker tool, not by hand.
├── design/
│   ├── main_page_directions.html  # TRACKED — static reference mockups (3 color/type directions, 3 photo-integration layouts) for #47, not linked from index.html or built by any script
│   ├── day_layout_mockup.html      # TRACKED — #47 mockup (PR #76): shared hero+banner top, Richmond day as Option A (header thumbs) vs Option B (photo per item), phone frame; Option B was picked — see section 4
│   ├── palette_explorations.html  # TRACKED — round-2 mockups for #47 (5 palette directions: Cloud Dancer, Cotswold Stone refined, English Rose, Racing Green, Terracotta Memoir) with 7 real trip photos embedded as data URIs; also published as an Artifact
│   ├── layout_explorations.html   # TRACKED — #92, planning only: 4 full structural layout directions (The Route, Field Notes, Corkboard, Night Reel), not a palette reskin — see section 6
│   ├── layout_explorations_2.html # TRACKED — #92 follow-up: The Route scaled to the whole trip + two siblings each of The Route (The Underground) and Field Notes (Postcards, Exhibition Placard)
│   └── underground_map_deepdive.html # TRACKED — #92 deep dive on The Underground (the direction that tested best): two more versions in genuine Beck-diagram grammar, not wired into index.html
├── docs/
│   └── HOW_IT_WORKS.md        # TRACKED — crash course on the repo's tech for a Python reader (linked from README)
└── README.md                  # refreshed 2026-07-13: live URL + structure + docs link
```

**Why `photos_raw/` and the manifest are gitignored:** the repo is public (required for free GitHub Pages hosting). Raw unreviewed photos and a CSV of precise GPS coordinates shouldn't be pushed before the user has curated them. Final, reviewed photos go in a separate tracked `photos/` folder once ready.

---

## 3. Itinerary HTML Handling

**Status: done.** The user pasted the original pre-trip itinerary HTML directly into chat (2026-07-12). It was saved verbatim to `original/itinerary_source.html` and used as the source for a redacted, restructured `index.html` — see section 4 for what that restructuring actually looks like. The workflow below is what to follow if the source HTML ever needs to be re-supplied or corrected.

The original pre-trip itinerary HTML (tracked in [#2](https://github.com/harveystephenson/London-2026/issues/2), closed) contained sensitive personal details that must **never** be committed to this public repo: flight booking references (PNRs), hotel/train/tour confirmation numbers, partial payment card digits, and a tour operator's personal phone number, among others.

**Handover method:** the user pastes the raw HTML directly into chat — don't scrape it via browser automation. An earlier attempt to pull it from its `claude.ai/public/artifacts/...` URL via the in-app browser was stopped by the user mid-attempt as less safe than a direct paste (see gotcha in section 9). Default to asking the user to paste content directly when there's a choice.

**Preserve the unredacted original:** when the user pastes the raw itinerary HTML, save it verbatim to `original/itinerary_source.html` (gitignored). This is a preservation copy so the user has the full original in one place if they ever want to reproduce it — it is **never modified** except when the user explicitly directs a fix (e.g. a schedule detail that changed after the trip). The site's `index.html` is a separate, derived, redacted file built from this source — not the same file, and not where source-of-truth edits happen.

**Redaction rule:** before committing any version of this HTML (or any derived file) to the repo, strip:
- Flight/train/restaurant/tour booking references and confirmation numbers
- Partial or full payment card digits
- Personal phone numbers of hotel/tour/venue contacts
- Any other unique identifier that could be used to look up or interfere with a real booking

Keep the unredacted original **out of git entirely** — don't commit it, even temporarily on a branch. If it needs to be saved to disk at all, keep it outside the repo or in a gitignored location.

After redacting, report a **high-level summary of what categories were stripped** back in chat — not the actual values — since the chat transcript itself shouldn't contain the sensitive detail either.

**Verification habit:** before trusting that redaction actually worked, audit the full git history (`git log --all -p | grep <sensitive-value>` across every commit/branch/file), not just the current working tree — confirmed clean this way on 2026-07-12 before a round of site restructuring, no history rewrite was ever needed because `index.html` was written pre-redacted from its first commit.

---

## 4. Site Structure & Content Decisions

`index.html` is a single static page, two JS-toggled tabs (`showTab()`), no separate page loads:

**Tab 1 — "Itinerary + Map"**, top to bottom:
1. Hero (title, dates, "Betty &amp; Hugh" — name order and no-hotel per #45, applied 2026-07-13). **#47, PR #81:** eyebrow text is "A Trip Remembered"; below the hero sits a 5-photo mosaic banner (1 large lead + 4 small, identical across every theme) and a small theme-picker strip (Terracotta / English Rose / Racing Green / Dark swatches).
2. **Map** — deliberately placed here, above "At a Glance," so it's the first real content visitors see. Leaflet.js + CartoDB Voyager tiles (colorful, retina-aware via `{r}` — an earlier CartoDB dark-tile attempt rendered as solid black and had to be reverted). Markers are single-style 🚩 flag-emoji `L.divIcon`s (not colored circles — no more confirmed/pending/free status coloring, since that stopped being meaningful post-trip). Three region tabs above the map (London / Cambridge / Cotswolds) just change `setView()` center/zoom — all markers exist regardless of which region tab is active.
3. **At a Glance** — one-line-per-day overview grid. Rows are clickable (#29, 2026-07-13): `jumpToDay()` opens and scrolls to that day's `<details>` block with a highlight flash.
4. **Day by day** — 10 collapsible `<details>` blocks, **all open by default since PR #70 (2026-07-13, user preference: expanded on load, click to collapse)**: the 9 trip days plus `day-10-jul` ("Dinner in DC at Rania"). **Fully reconciled against the user's row-by-row directions on 2026-07-13 (PR #64):** every `final_location` tag now has a schedule line, rows are photo-time-ordered, and the Cotswolds day lists all eight tour stops individually (Broad Campden, Broadway Tower, Broadway, Snowshill, Upper Slaughter, Lower Slaughter, Burford, Bourton-on-the-Water) plus the lunch stop. Deliberately stripped down from the original pre-trip planning doc for a family/friends retrospective audience: no Cayman Islands time column, no sunrise/sunset/daylight strip, no "Alt: do X instead" suggestions, no CONFIRMED/NOT BOOKED/CHECK NOW status badges or pills, no per-day Confirmations/warning boxes. Kept: activity text and photo links (**times removed entirely per #43, 2026-07-13** — events appear in sequence only; flight times still live in Confirmed Bookings), and a "📺 Videos by Others" strip per day (renamed from "Watch Before You Go" — purpose is added context/color, not pre-trip research; user finds this label inelegant, rename tracked in [#31](https://github.com/harveystephenson/London-2026/issues/31)). **Photo links are photo-first as of PR #56 (2026-07-12, explicit user direction: "emphasis on the photos, not the travel guidance"):** the event *name itself* is the link (`.act-link`, dotted gold underline + small 📷 suffix) opening the per-event grid popup scoped to that location — see section 5 — falling back to the whole day's photos otherwise — **as a thumbnail-grid popup (`openDayGrid`), not the single-photo lightbox**, since 2026-07-13 (user: clicking a link should always land on a grid of thumbnails first). All four day-level entry points (day-07 flight rows, Rania row, pin-popup fallback, empty-location-grid fallback) go through it. All dynamically built photo URLs carry `PHOTO_V` (`?v=2`, PR #75) — see the cache gotcha in section 9; official websites (Kew, Petersham, Ambassadors, Sky Garden) are demoted to a small 🔗 icon after the name. The Liverpool Street row links straight to day-level photos because its id has no flag (was a dead-end popup before #56). Map pin popups likewise lead with "📷 View N photos" (real per-location count) and list "Jump to this day →" second. **#47, PR #81 — Option B shipped:** every schedule row with a location pin (`<tr id="loc-X">`) gets a second `<td class="item-photo">` cell with a clickable thumbnail (desktop/tablet only, hidden under 600px); on phones each day instead shows one photo under its date/title (`.mob-thumb`). Both are populated by `build_gallery.py` from `data/featured_photos.csv`, falling back automatically (day → that day's first photo chronologically; item → that location's first photo) so an un-picked slot never breaks — see section 4's Theme & Featured Photos block below and section 6.
5. **Confirmed Bookings** — flights + hotel only, no confirmation numbers, deliberately placed last (was first in the original pre-trip doc).

**Theme switcher & featured photos (#47, PR #81, 2026-07-13) — fully shipped, not just planned:**
- **Four themes**, pure CSS-variable swaps via `:root[data-theme="terracotta|rose|green|dark"]` on `<html>`. **Dark is the default** (as of PR #82, 2026-07-13) — the plan flip-flopped mid-session: originally dark, then changed to Terracotta Memoir once the mockups shipped (PR #81), then reverted back to dark after the user saw it live ("sorry to keep changing my mind" — no apology needed, this is exactly what the mockup/picker tooling exists to make cheap). The bare `:root` selector always targets whichever theme is current default — swapping it again later is a one-block CSS move plus updating the JS/comment fallbacks that mention the theme name by string, not a rewrite. A small swatch-strip picker sits under the hero banner; choice persists to `localStorage` under `site-theme`, restored via a tiny synchronous script in `<head>` (before first paint, so there's no flash of the wrong theme on reload).
- Each theme also carries its own hand-tuned gradient stops (`--hero-grad-1/2`, `--strip-grad-1/2`) and hover tint (`--row-hover`) — these were hard-coded hex values in the original single-theme CSS and had to be pulled into variables; if a future palette tweak needs a new gradient, add it the same way rather than hard-coding.
- **`data/featured_photos.csv`** (`role,filename,day,location_id,sort,crop_x,crop_y`) drives three photo slots: `banner_lead` (1) + `banner` (4, ordered) for the hero mosaic; `day_feature` (0–1 per day) for the mobile one-photo-per-day; `item_feature` (0–1 per schedule-linked location) for the Option B per-item photo. **The `filename` column holds a lowercase SLUG** (`stem_slug` output, e.g. `img_2830`), not the raw manifest filename — the picker tool only has `{thumb,full}` paths to work from, and slugs are already the pipeline's canonical join key (same one `build_gallery.py` uses for output paths). Seeded with the 5 banner photos from the original terracotta mockup (lead `img_2830`, then `img_3374`/`img_2493`/`img_3027`/`img_2536`). **User's first picker pass applied 2026-07-13 (PR #82):** all 10 day photos and 31 of 44 item photos got explicit picks. **User's second pass applied 2026-07-14 (direct-to-master, commits `badbe77`/`bf72a9a`):** crop repositioning on the banner lead + 4 banner photos, all 10 day photos, and 39/44 item photos; banner lead/photo-4 also swapped. 5 item slots still ride the automatic fallback (that location's first photo, centered). Re-running the picker later reflects whatever's live at that point, so it's safe to have the user do further passes anytime without coordinating.
- **Drag-to-reposition crop (#84, PR #85, 2026-07-14):** `crop_x`/`crop_y` (0–100, default 50/50 = center) is an `object-position` percentage, added because the fixed-box `object-fit: cover` slots (banner, `.mob-thumb`, `.item-photo`) were cropping off-center photos arbitrarily — reported by the user specifically about the mobile daily picture, then extended to all three slots as the same underlying bug. In the picker, each slot preview is a `.crop-frame` div sized to (roughly) the slot's real on-site aspect ratio, and you **drag the photo inside the box** to reposition it — pointer-based drag math (`attachDrag` in the picker JS) converts a pixel delta into an object-position % using the same `object-fit: cover` scale math the browser itself uses, so what you see dragging matches the real render. `build_gallery.py`'s `crop_style_attr()` only emits the `style="object-position:…"` attribute when it differs from center, so untouched slots produce byte-identical HTML — verified via a full rebuild diff before shipping. **A new photo pick always resets crop to center** (an old crop was framed for a different photo).
- **`scripts/make_featured_picker.py`** generates a gitignored `photos_raw/featured_picker/picker.html` (serve via the `site` launch config) — one card per slot (5 banner + 10 day + 44 item, 59 total) showing its current photo (pick or fallback, in its crop frame) and a "Choose…" button that opens a candidate grid scoped to what's relevant (whole-trip with a day filter for the banner; just that day's photos for a day slot; just that location's photos for an item slot). Reads its candidate pools straight from the *live* `galleryData`/`galleryByLocation` JSON already baked into `index.html`, not recomputed — always matches exactly what's on the site. `localStorage` persistence; Export downloads `featured_photo_decisions.json` — **the browser numbers repeat exports** (`featured_photo_decisions (2).json`, `(3).json`, …) rather than overwriting, so always apply the **highest-numbered** file in `~/Downloads`, not the bare filename. `scripts/apply_featured_photos.py <json>` does a **full rewrite** of `featured_photos.csv` (not a merge — the picker's local state already holds the complete picture), then `build_gallery.py` bakes the picks into `index.html` (fast, data-only — no photo re-encoding needed for this).
- User's own framing, worth remembering: *"the photo review html has been a much better experience than the CSV"* — this tool exists because hand-editing `featured_photos.csv` would have been exactly the kind of friction the combined photo-review tool (section 5) already proved isn't necessary.
- **Still manual, tracked for later in [#86](https://github.com/harveystephenson/London-2026/issues/86):** every picker round today was drag → Export → find the right numbered file in Downloads → `apply_featured_photos.py` → `build_gallery.py` → commit/push, each step done by Claude on request. Automation ideas (watcher script on Downloads, File System Access API direct-write, live `index.html` preview in the picker, live-reload) are captured on the issue — not started.

**Tab 2 — "Photo Gallery"**: thumbnail grid grouped by day (`buildGalleryTab()`), click any thumbnail for a lightbox (prev/next buttons + arrow keys, Escape/click-outside to close). Gallery data is embedded **inline** in `index.html` between `/*GALLERY_DATA*/ ... /*END_GALLERY_DATA*/` markers, not fetched at runtime — see section 5, this was a deliberate fix for a real bug (blank gallery under `file://`/local-preview contexts).

**Itinerary corrected to match what actually happened**, not the original pre-trip plan (agreed 2026-07-12, applied directly as the user reported corrections — no formal audit/checklist was built, see section 6 for what's still unverified):
- Removed entirely (never happened): Pride Parade, the Chelsea breakfast, Churchill Arms, Harry Potter Studios, the Sketch dinner on 29 Jun (#41), the Botanical Brasserie breakfast and Peggy Jean dinner on 30 Jun, the Bridge of Sighs / Botanic Garden rows on 5 Jul, and the "Took Cutie to Liverpool Street" row on 6 Jul (all 2026-07-13, per user; none had tagged photos)
- Added: The Notting Hill Bookshop, High Tea at Jacqueline (The Chancery Rosewood), Battersea Power Station + Italian dinner on 6 Jul (#42), and — 2026-07-13, PR #64 — Clermont check-in, Royal Exchange, 10 Downing Street, St James's Park, Piccadilly Circus, Harrods (store), Hampstead Village, Notting Hill, Kensington Gardens, Cambridge Station, all eight Cotswolds stops + lunch stop, and the Rania DC day
- Retimed: Notting Hill/Portobello Road to morning, Dishoom Permit Room to lunch (were afternoon/dinner in the plan)
- The whole "Reference & Ideas" section (action items, restaurant wishlist, useful links) was deleted outright — pre-trip planning content with no place on a retrospective site

---

## 5. Photo Pipeline

- **Source is currently a manual USB import, not iCloud (temporary, as of 2026-07-12).** iCloud Photos sync got stuck/paused mid-trip and was missing every photo from three full days — Fri 3 Jul (Cotswolds), Sun 5 Jul (Cambridge), Mon 6 Jul — even after being unpaused and left overnight. Root cause suspected to be iCloud storage or a stuck upload queue on the phone; not fully resolved. Worked around by connecting the iPhone directly via USB, using Windows' "Import photos and videos" wizard, and dumping everything into `photos_raw/iphone/`. `scripts/extract_trip_photos.py`'s `SOURCE_DIR` was repointed at `photos_raw/iphone/` accordingly (see comment in the script) — **switch it back to `C:\Users\harve\iCloudPhotos\Photos` once iCloud sync is actually fixed**, then re-run and diff against the current state as a sanity check.
- `scripts/extract_trip_photos.py` reads EXIF `DateTimeOriginal` + GPS from each file (via `pillow-heif` for HEIC support) and copies matches into `photos_raw/uk_trip` or `photos_raw/dc_reunion` based on date windows (padded a day or more on each side of known travel dates). Writes `data/photo_manifest.csv`.
- Fast and idempotent: a filesystem-mtime pre-filter skips full EXIF decode for files nowhere near the trip window, and rows already recorded in the manifest are reused on re-run rather than re-decoded (see #12/#13 in section 9).
- **Re-run safe with source switches, non-destructively:** any file present in an older source (e.g. iCloud) but missing from whatever the current `SOURCE_DIR` is gets *moved* — not deleted — into `photos_raw/pruned_review/`, for manual review rather than silent loss. See #24: switching to the USB import lost 226 files (189 photos + 37 screenshots) this way, likely from a manual deletion pass the user did the same morning — needs a look before being discarded for good.
- Manifest columns: `filename, category, datetime, lat, lon, suggested_location, final_location, deleted, source` (`source` since PR #78: blank = user's phone, `moms_phone` = mom's WhatsApp export). `suggested_location` is Claude's guess (via `scripts/suggest_photo_locations.py`, nearest known flag by day + GPS); `final_location` is the user's own call and is what actually drives the site. **`final_location = "Ignore"`** is a reserved value meaning keep the photo but never show it on the site — `build_gallery.py` excludes those rows entirely (28 photos as of 2026-07-12), distinct from `deleted` (physically removed from `photos_raw/uk_trip`).
- **Mass edits are folder-native since #61 (2026-07-13): both deleting from AND adding to `photos_raw/uk_trip` / `photos_raw/dc_reunion` are tracked** — a file added directly there (not in manifest or source) is adopted on the next `extract_trip_photos.py` run (EXIF read, mtime date fallback for WhatsApp-style files), and a previously deleted file placed back is un-deleted. Never edit `photos_raw/iphone` for curation.
- **To delete photos: delete from `photos_raw/uk_trip` (or `photos_raw/dc_reunion`), never from `photos_raw/iphone`.** `photos_raw/iphone` is the raw source dump `extract_trip_photos.py` reads from — deleting there just makes the file silently vanish from the source scan, not marked deleted, and re-running the script would likely sweep the still-present curated copy into `pruned_review/` with no record it was intentional. Deleting from `photos_raw/uk_trip` instead gets tracked properly: the next `extract_trip_photos.py` run marks that row `deleted=true` in the manifest (kept, not removed) instead of re-copying it back in from the source.
- **Grouping by location is exact-string match against `data/flag_locations.csv`'s `name` column — strip whitespace before comparing.** Hand-typed CSV values are prone to stray leading/trailing spaces (bit us on 2026-07-12: `"Bank of England "`, `"Battersea Power Station "` silently failed to group until `build_gallery.py` started stripping both sides before the lookup). If a `final_location` looks like it should be grouping but isn't, check for this first.
- **Don't add every `final_location` value to `flag_locations.csv` blindly — check geography first.** `Heathrow Airport` and `Dulles Airport` are deliberately excluded from ever becoming map pins (per explicit user instruction, 2026-07-12) — they still group fine by day, they just never get flag/lat-lng entries. Dulles especially: a Virginia coordinate on a UK-focused map would badly skew any future auto-bounds-fitting. Same caution applies to `Rania, DC` (already naturally excluded since it's `category: dc_reunion`, not `uk_trip`).
- `scripts/build_gallery.py` converts `photos_raw/uk_trip` into web-ready JPGs (thumbs ~400px, full ~1600px/quality 75 — deliberately lower than Pillow defaults to keep git history growth reasonable, ~213MB for 591 photos instead of ~346MB) under `photos/<day-slug>/`, and **injects the resulting JSON directly into `index.html`** between `/*GALLERY_DATA*/ ... /*END_GALLERY_DATA*/` markers rather than having the page `fetch()` it at runtime — `fetch()` of a local file is blocked under `file://`, which broke the gallery when viewed through a local preview panel instead of a real HTTP server. Also prunes (deletes — these are regenerable derived images, not originals) any thumb/full JPG no longer in the manifest. Incremental + parallel since #54/PR #55 (see section 6): data-only rebuilds take ~3s, and only photos whose outputs are missing/stale get re-encoded.
- **Photo enhancement pipeline (2026-07-13, PR #71):** `scripts/photo_enhance.py` holds the single deterministic recipe (autocontrast + mild color/sharpen); `build_gallery.py` reads **tracked** `data/photo_edits.csv` and applies `enhance()` at encode time to approved filenames — so approvals survive every rebuild including `--force`. **Review complete 2026-07-13: user approved all 717 photos** (recipe v2, the skin-friendly one); applied via PR #74 — every shipped JPG is the enhanced version. The original single-purpose review scripts (`make_edit_review.py`/`apply_photo_edits.py`) were superseded by the combined tool below (PR #78); the old `photo_edit_decisions.json` format is gone with them.
- **Combined photo review tool (2026-07-13, PR #78, #77):** `scripts/make_photo_review.py` generates gitignored `photos_raw/photo_review/review.html` (serve via the `site` launch config, localhost:8123) — one card per reviewable photo (user's + mom's + previously-Ignored): a **location dropdown** prepopulated with all existing categories (flag names + manifest `final_location`/`suggested_location` values + Ignore, current value preselected) plus an add-a-category box (case-insensitive dupe guard) so categories are picked, never retyped; a **quality before/after with Approve/Reject** only where `photo_edits.csv` has no decision yet (already-decided photos show one site thumbnail — keeps the page light); filters for mine/mom's, quality-undecided, location-unassigned, location-changed; localStorage persistence. Export downloads `photo_review_decisions.json` (quality lists + **only changed** locations + new categories); `scripts/apply_photo_review.py <json>` (BOM-tolerant, defaults to `~/Downloads/`) merges quality into `photo_edits.csv` (deleting stale derived JPGs) and writes location changes into the manifest's `final_location`, then reports categories with no flag yet and any mom's tags with no derivable day. Then `build_gallery.py`. **Status: built and on standby — the user's actual pass hasn't happened yet (#77 open).** As generated: 854 cards, 137 quality-pending (109 mom's + 28 Ignored), 109 locations unassigned.
- **Mom's photos (PR #78, closed #6):** `photos_raw/moms_phone` holds mom's WhatsApp export — **zero EXIF, 900×1600 downscaled, filename timestamps are the SEND time (2026-07-08, post-trip), not capture time**, so no time/GPS matching is possible; tagging is fully manual via the review tool. 109 unique photos adopted into the manifest (`source=moms_phone`, original filenames kept — no renaming); 25 exact byte-duplicates auto-moved to `photos_raw/moms_phone_dupes/`; 2 MP4 clips parked untouched (noted on #31). The folder has the same curated-folder semantics as `uk_trip` (adopt/delete-track/un-delete). **Their day placement comes from the assigned location** — flag day, else the modal day of the user's photos with the same `final_location` — and they sort after the user's photos within each day, mixed in with no visible distinction (user decision 2026-07-13). Unlocated mom's photos stay off the site (build prints a count). Output stems are slugged URL-safe (spaces/parens collapsed; identity for `img_xxxx`) with a collision guard in `build_gallery.py`.
- Only `uk_trip` photos dated within the 9-day itinerary window (2026-06-29 to 2026-07-07) make it into the gallery — pre-departure prep shots and post-trip photos are matched by `extract_trip_photos.py`'s wider date padding but excluded by `build_gallery.py`, since they don't map to any day section on the site.

**Current state (as of 2026-07-13, end of session):** **797 photos live on the site** (786 UK trip + 11 DC/Rania), up from 717 this session via the first mom's-photo review pass (PR #79) — 686 are the user's own, 111 are mom's (all but 2 of her 109 tagged photos placed; the other 2 are outside the 9-day window or off a dayless category, see section 6). **759/797 group by specific location across 44 map pins, all populated.** The 38 ungrouped: 22 generically-tagged "Cotswolds" (no specific village), 11 Rania (by design, no pin), 5 Heathrow (day-level, deliberate). Photo links and map pins open a per-event grid popup scoped to that specific location, falling back to the whole day's photos where no match exists yet. The user's manual `final_location` curation pass ([#38](https://github.com/harveystephenson/London-2026/issues/38)) is done for their own photos; mom's photos get the same treatment through [#77](https://github.com/harveystephenson/London-2026/issues/77) (open, more passes expected). **#47 (theme switcher + featured photos) shipped this session** (PR #81) — see section 4's "Theme & Featured Photos" block. **The site is live** at https://harveystephenson.github.io/London-2026/ (see section 7). Still pending: the `pruned_review/` reconciliation ([#24](https://github.com/harveystephenson/London-2026/issues/24)), pin-crowding/overlap cleanup ([#50](https://github.com/harveystephenson/London-2026/issues/50)), and the Battersea Italian restaurant name. Build speed ([#54](https://github.com/harveystephenson/London-2026/issues/54)) was fixed 2026-07-12 — see section 6.

**Known gap:** WhatsApp-forwarded photos (from mom) have no EXIF at all (WhatsApp strips it on send) — these always need manual location tagging, which is exactly what the combined review tool (PR #78) exists for. Expectation set by the user: more of mom's photos will end up `Ignore`d than their own.

---

## 6. Open Items / Deferred Decisions

*Do not act on these without the user raising them again — they're tracked here so they aren't lost, not so they're auto-implemented.*

- **iCloud sync still broken** — unresolved as of 2026-07-12. Once fixed, switch `extract_trip_photos.py`'s `SOURCE_DIR` back to the iCloud folder (see section 5) and diff against the USB-sourced state.
- **`photos_raw/pruned_review/` reconciliation** — 226 files (189 photos + 37 screenshots) need manual review to confirm they were intentionally deleted vs. accidentally dropped by the USB import. Tracked in [#24](https://github.com/harveystephenson/London-2026/issues/24). **2026-07-14 addition:** user wants this built as a section inside an existing review HTML tool (photo-review or featured-photo picker) rather than a standalone contact-sheet page, whenever it's picked up — see the issue comment.
- **Branch protection on `master`** — GitHub flags it as unprotected. Tracked in [#20](https://github.com/harveystephenson/London-2026/issues/20) with specific recommended settings (restrict deletions, block force pushes; skip PR-required/status-checks given the existing workflow and no CI). Not urgent for a solo repo.
- ~~Cotswolds lunch restaurant~~ — **resolved 2026-07-13 (PR #79):** The Feathered Nest Inn, Nether Westcote OX7 6SD. `cotswolds-lunch` flag renamed and its coordinate corrected to the postcode centroid (51.8807,-1.6745 — was a photo-GPS estimate ~250m off); Fri 3 Jul schedule row updated with the name and a photo link. The Italian dinner at Battersea Power Station (Mon 6 Jul) is still unidentified.
- **Itinerary accuracy — partially done, not exhaustively verified.** Known-wrong items the user explicitly flagged have been corrected (see section 4), but this was reactive, not a systematic pass — there could be other inaccuracies nobody's caught yet. [#8](https://github.com/harveystephenson/London-2026/issues/8) stays open for that reason.
- **DC Rania photos placement:** grabbed into `photos_raw/dc_reunion/` but not yet decided how (or whether) they fit into a UK-focused map. Likely a non-map "postscript" section rather than a pin, but undecided.
- ~~Mom's photos~~ — **resolved 2026-07-13 (#6 closed, PR #78):** arrived as a WhatsApp export despite the metadata-stripping (user accepted the quality tradeoff); ingested per section 5. The iCloud Shared Photo Library option remains a possible later quality upgrade, but nothing is planned. **First review pass applied 2026-07-13 (PR #79):** 819 photos approved for enhancement (4 rejected, all mom's — brand-new to the site, so no `PHOTO_V` bump needed), 109 mom's photos assigned a location, 80 now live (1 stays unplaced: a single pre-departure Dulles-lounge photo, outside the 9-day window — same as the user's own Dulles shots, not a bug). The export surfaced three new categories, resolved via chat + web research rather than guessed: **CCSS** (user's old school, now The Perse School, 13 Station Road — flag placed from the postcode centroid since mom's photos carry no GPS), **Coe Fen** (a near-duplicate "Cambridge Cattle Market" tag turned out to actually be Coe Fen, the grazing common by Mill Lane punting station — confirmed with the user before renaming), and **Cambridge University Arms Hotel** (initially flag-only per the user's "just a photo stop" read of their answer; they clarified 2026-07-13 they did want a schedule row after all — added in PR #80, positioned between punting and the city-centre walk based on send-order timing + geography). All three — CCSS, Coe Fen, and University Arms Hotel — now have schedule rows on day-05-jul: Cambridge Station → CCSS → Coe Fen → Punting → University Arms Hotel → City Centre. [#77](https://github.com/harveystephenson/London-2026/issues/77) stays open — this was one pass, not the final one; more location corrections may follow.
- **Blur/bad-photo triage:** planned but not built — a script to flag likely-blurry shots (e.g. Laplacian variance) to speed up the user's manual review pass. Final delete decisions stay with the user. Tracked in [#7](https://github.com/harveystephenson/London-2026/issues/7).
- ~~Cotswolds villages~~ — **resolved 2026-07-13** (#53 closed, PR #64): GPS/time clustering of all 133 day-03 photos found real clusters for every remaining stop — Snowshill (7), Upper Slaughter (6), Lower Slaughter (1), Bourton-on-the-Water (38, the big final stop), plus a 19-photo 12:48–14:38 cluster at (51.8818,-1.6725) near Westcote that is almost certainly the missing lunch restaurant (flag `cotswolds-lunch`, "name TBC"). Assignments went into `suggested_location` only (the generic `final_location = "Cotswolds"` falls through to it, so the user's column was never touched). Only Chipping Campden and Moreton-in-Marsh ended up with zero photos (3 en-route car shots left unassigned). **User caveat (2026-07-13): their phone GPS was sometimes off that day, so individual cluster assignments may be wrong** — the correction path is the user setting `final_location` on the specific manifest row (it beats `suggested_location`), then a ~3s data-only rebuild; don't re-run coordinate clustering to "fix" individual photos. Original context follows: Of the 8 tour-guide stops from #39, only 3 ended up with any matched photos (Broad Campden: 1, Broadway Tower: 22, Broadway: 32); the other 5 (Chipping Campden, Snowshill, Upper Slaughter, Lower Slaughter, Bourton-on-the-Water) had zero matches and were removed as flags in #52. (Burford — 2 matches — and Moreton-in-Marsh — 0, also removed — are separate pre-existing flags, not part of the 8-stop list; 57/133 Cotswolds photos are matched in total across all flags.) **User is confident they have photos from every stop on the tour-guide list**, not just 3 — this is a real discrepancy to investigate, not assumed absence. The `broadway-tower` flag itself was a worked example of the likely fix: it also showed zero matches until the actual GPS cluster was found by hand (a tight 22-photo, 16-minute cluster ~2.5km from the original hand-estimated coordinate) and the flag's lat/lng corrected to match reality. Same technique likely needed for the other 5: pull unmatched Cotswolds photos chronologically with GPS, look for tight time/location clusters, recompute flag coordinates from real cluster centroids rather than estimates. See #53 for the exact investigation steps.
- **Typo/duplicate check on `final_location`, still not done.** Ran an eyeball pass during the reconciliation on 2026-07-12 (caught 5 real naming mismatches, fixed in #49) but never ran a systematic fuzzy/case-insensitive dedup pass over the full column. Worth doing before the next round of map changes.
- ~~Map reshaping / main-page photo placement (#47)~~ — **shipped 2026-07-13 (PR #81), #47 closed.** Pin/tooltip work from earlier (36→44 pins as new locations were added, see section 5) stays as-is; this was the theme-switcher + photo-placement half. **Option B won** (photo beside every schedule item, not header thumbnails) — see `design/day_layout_mockup.html` for the mockup that settled it; all four themes ship, user-switchable, persisted to `localStorage`. **Default theme is dark** (PR #82) — flip-flopped from dark → Terracotta Memoir (PR #81, seeing the mockups) → back to dark (PR #82, seeing it live) within the same session; see section 4 for why that's cheap to keep changing. The three palette preview files (`index-rose.html`/`index-green.html`/`index-terracotta.html`) and their one-off generator are gone — superseded by the real `[data-theme]` implementation in `index.html`. Full technical writeup is in section 4's "Theme & Featured Photos" block. **What's genuinely new here, not in the original plan:** the featured-photo *picker tool* (`make_featured_picker.py`/`apply_featured_photos.py`) — the original plan only specified the CSV, but the user explicitly asked for a picker UI ("make it clear where I can choose photos... I'll let you think about how to design that") after the combined photo-review tool worked out well. **First picker pass applied 2026-07-13 (PR #82):** all 10 day photos + 31/44 item photos now have explicit picks; the banner lead and remaining 13 item slots still ride the automatic fallback — nothing broken by that, more passes can happen anytime.
- ~~Build speed~~ — **fixed 2026-07-12** ([#54](https://github.com/harveystephenson/London-2026/issues/54), PR #55): `build_gallery.py` is now incremental (skips any photo whose thumb/full JPGs are newer than the source) and parallel (ProcessPoolExecutor for photos that do need work; thumbs derive from the 1600px full, not the ~12MP original). Data-only rebuilds (`final_location` edits, flag changes) run in ~3s instead of 7+ min, and OneDrive no longer re-uploads ~267MB of identical JPGs after every run. `photos/.build_settings.json` stamps the size/quality constants — changing them (or passing `--force`) triggers a full rebuild. (The old "first full rebuild rewrites everything" caveat is spent: the 2026-07-13 enhancement pass re-encoded all 1,434 JPGs anyway, and encoding is deterministic, so a `--force` now costs only time + OneDrive churn, not surprise git churn. **If a mass re-encode ever changes bytes again, bump `PHOTO_V` in index.html** — section 9.)
- **Multi-trip expansion (Italy, DC, other trips with mom) — planning only, tracked in [#57](https://github.com/harveystephenson/London-2026/issues/57).** No source HTMLs exist for those trips, but iPhone photos with EXIF do. The issue captures the open questions: site structure (trip switcher over more tabs), reconstructing itineraries from EXIF timelines + GPS clusters (suggest/confirm pattern), generalizing the three London-hardcoded scripts behind a per-trip config, privacy/repo-size. Suggested sequencing inside: photo inventory first, pilot with DC before Italy. **2026-07-14 addition — scope is at least 5 trips total, not 2-3:** (1) all of mom's DC trip(s), as their own page — broader than the existing Rania-dinner postscript on this site, which stays as-is; (2) the user's own DC days out, a **separate** page from mom's DC trips even though same city; (3) Italy with mom; (4) France with friend Ridhiima. **Standing privacy rule for (4), same severity as this repo's PNR/payment-card redaction rules: no photos of Ridhiima go anywhere, including git history, not even gitignored, unless the user explicitly confirms she's given permission** — needs its own manual sign-off review pass before any push once that trip is picked up, separate from the general photo-review pipeline. (Also saved to Claude's cross-session memory so it survives even without rereading this issue.) **Do not start any of this without the user picking it back up.**
- **Holistic review recommendations (2026-07-12) not yet tracked as issues** — raised in chat, user hasn't dispositioned them; don't act unprompted, but these are the ones worth re-suggesting at natural moments:
  - `suggest_photo_locations.py` same-day-only matching gap: when a photo's day has flags, there's no fallback to all-flags, so e.g. hotel photos taken mid-trip can never match `clermont-victoria` (a day-29 flag). Fix: fall back to all flags when the nearest same-day flag misses the 600m tolerance. May explain unmatched photos beyond the Cotswolds; pairs naturally with #53.
  - `tripLocations` in `index.html` duplicates `data/flag_locations.csv` (two sources of truth, already bitten once by the Broadway Tower fix needing both) — inject the array from the CSV via a third marker pair in `build_gallery.py`.
  - Add `<meta name="robots" content="noindex">`, Open Graph tags + favicon (share-preview quality for family iMessage/WhatsApp shares).
  - ~~`favorite` column~~ — superseded by the `featured_photos.csv` plan on #47.
  - Mobile lightbox pass: swipe gestures + adjacent-image preload (fold into #32); check map one-finger-drag vs page scroll during #11 QA.
  - Merge #8 + #33 into one EXIF-vs-schedule audit; #43 (remove times) landed 2026-07-13, which shrinks #33 to sequence-checking.
  - #31 videos: settle hosting before building (unlisted YouTube recommended over committing MOVs — GitHub blocks >100MB files, repo ~1GB soft limit).
  - ~~#50 nudge + tooltips~~ — done 2026-07-13 (PR #66).
  - ~~contact sheet~~ — built 2026-07-13 (PR #68, `scripts/make_contact_sheet.py`); all 226 decoded, **zero are from 5 July**, so the missing Dishoom Cambridge shots are NOT in quarantine (noted on #48). User's keep-or-discard skim (#24) still pending.
- ~~Arbitrary photo cropping (#84)~~ — **shipped 2026-07-14 (PR #85).** See section 4's "Drag-to-reposition crop" block for the technical writeup. Follow-up automation ideas tracked separately in [#86](https://github.com/harveystephenson/London-2026/issues/86) (not started).
- **Dulles Airport pre-departure photos invisible on the site — planning only, tracked in [#87](https://github.com/harveystephenson/London-2026/issues/87).** 4 photos (3 the user's + 1 mom's, all "Dulles Airport (BA Lounge)") are dated 2026-06-28, one day before `DAY_SLUGS` starts (2026-06-29), so `build_gallery.py`'s day-assignment drops them entirely — this is also why mom's one "unplaced photo" the build reports is always this same file. Ask: a "Dulles" row on Monday 29 June including all 4. Needs a location-specific day-override in `build_gallery.py`, not a general window widening (don't want to pull in other pre-departure noise). No map pin either way — Dulles stays excluded from `flag_locations.csv` per the existing rule.
- **Visitor analytics — planning only, tracked in [#90](https://github.com/harveystephenson/London-2026/issues/90).** GitHub's repo Insights → Traffic tab already covers repo-level views (owner-only, 14-day window, no country data) with zero code — that's separate from tracking visits to the live site itself. Recommendation if picked up: GoatCounter (privacy-friendly, no cookie-consent banner needed, gives country-level breakdown without storing raw IPs) over Google Analytics (needs a consent banner) or Cloudflare Web Analytics (harder to wire the self-exclude flag into). Design includes a **self-visit exclude flag** so the user's own laptop/iPhone/iPad don't count as visitors: disable GoatCounter's auto-onload tracking, check a `localStorage` flag before calling `window.goatcounter.count()` manually, and set that flag permanently via a one-time `?me=1` link visited once per browser/device. IP-based exclusion was considered and rejected — doesn't survive a phone/iPad roaming between wifi and cellular. **Blocked on the user creating a GoatCounter account and site code** (Claude doesn't create accounts on the user's behalf) — full sign-up link and next steps are at the top of the issue.
- **Structural layout redesign — planning only, tracked in [#92](https://github.com/harveystephenson/London-2026/issues/92). User has framed this as "a kind of side project," not on the critical path.** Separate from the palette/photo-placement work already shipped in #47 — this is about breaking `index.html` out of its current grid/card structure (hero → tabs → table-grid → collapsible list) entirely. Three rounds of static mockups exist in `design/` (see section 2): `layout_explorations.html` (4 initial directions — The Route, Field Notes, Corkboard, Night Reel — with a trade-off comparison matrix), `layout_explorations_2.html` (drills into the two that tested best: The Route scaled to the full ten-day trip with a sticky progress rail, plus Underground/Postcards/Exhibition-Placard siblings), and `underground_map_deepdive.html` (two more Beck-diagram-grammar versions of The Underground, the single strongest direction — Refined Network vs. the busier, less-accurate-to-the-trip Full Network). No decision made, nothing wired into `index.html`. **When picked back up, start with the question in #92's closing section:** does the site move wholesale to one structure, or does it stay the current grid with an idea or two borrowed (e.g. the sticky progress rail, or an Underground-style diagram as a supplement to the existing Leaflet map rather than a replacement)?
- **Two off-topic issues also live in this repo's tracker, not site work:** [#88](https://github.com/harveystephenson/London-2026/issues/88) (a reusable CLAUDE.md template chunk for issue/PR conventions, for use across the user's other projects) and [#89](https://github.com/harveystephenson/London-2026/issues/89) (reading the user's Microsoft To Do list of future Claude Code project ideas — no MCP connector exists for this yet, checked 2026-07-14). Both planning-only, filed here because this is the user's working issue tracker with Claude, not because they relate to this site. Don't action either without being asked.

---

## 7. Hosting

**Live since 2026-07-12: https://harveystephenson.github.io/London-2026/** — GitHub Pages, off the `London-2026` public repo, serving `master` from `/`. Free tier requires the repo to stay public — user has confirmed this is acceptable for this project. (User's other, unrelated repos are separate and their visibility is the user's own responsibility to manage — not something to be changed on their behalf.)

**Auto-rebuilds on every push to master** — no separate publish step needed; a merged PR is live within a minute or two.

**No private staging exists on the free tier for a public repo** — enabling Pages is the same action as publishing to the world (the URL is just unguessable/unlinked, not access-controlled). User explicitly chose "publish as-is, known gaps included" over a pre-publish cleanup pass, and shares the link with family themselves — not something to send on their behalf. Enabled via `gh api repos/harveystephenson/London-2026/pages -X POST -f "source[branch]=master" -f "source[path]=/"`.

---

## 8. Development Workflow

**Push periodically via pull request — this is a standing user preference, not a one-off request.**

- Config/reference files (`CLAUDE.md`, `.gitignore`, `README.md`) may be pushed directly to `master`.
- Everything else (scripts, site code, content) goes through a feature branch + PR:
  ```
  git checkout -b feature/short-desc
  # commit changes
  git push -u origin feature/short-desc
  gh pr create --title "..." --body "..."
  ```
- **If the change relates to an open issue, link the PR to it** — include `Closes #N` (or `Refs #N` if it only partially addresses the issue) in the PR body so GitHub attaches them automatically. Check open issues before opening a PR if it's not obvious which one applies.
- **PRs are for tracking, not per-PR review** — the user does not want to review each PR individually. Merge once the PR is open, mergeable (clean, no failing checks if any are configured), and its issue links are in place — no need to ask for confirmation first. The user's engagement in this chat/session is standing approval for the work being merged.
- **Close issues proactively when the related work merges** — don't wait to be asked. Use `gh issue close <N> --comment "..."` with a comment covering: (1) what was implemented, (2) the key design decisions, (3) the relevant commit hash(es) or PR number. Do this immediately after the relevant work lands.
- **No plan-first gate for this project.** Unlike macro_consensus, implement directly off the user's requests (branches, commits, PRs) rather than proposing a plan and waiting for approval first.
- **Watch out for accidental direct-to-master commits** — happened once (2026-07-12) when a large restructuring commit was made without first creating a feature branch. Caught before pushing and moved onto a proper branch/PR, but double-check `git branch --show-current` before committing site-code changes.

---

## 9. Known Gotchas / Environment Notes

*Running log of environment traps hit during development — add to this as new ones surface.*

- `gh` is not on the system PATH on this machine. Use the full path: `C:\Program Files\GitHub CLI\gh.exe`.
- Almost all real iPhone photos are `.HEIC`, not `.jpg`/`.png` — Pillow needs the `pillow-heif` plugin (`pip install pillow-heif`, then `pillow_heif.register_heif_opener()`) to read them at all, EXIF included.
- The local iCloud Photos sync folder (`C:\Users\harve\iCloudPhotos\Photos`) is flat — no date-based subfolders, and filenames (`IMG_0044.PNG`, random UUIDs) carry no reliable ordering. Always filter by EXIF `DateTimeOriginal`, never by filename.
- WhatsApp strips all EXIF metadata (including GPS) on send — any photo forwarded via WhatsApp will need fully manual location tagging.
- `extract_trip_photos.py` was originally slow (30-40+ min against ~13K synced files): full-resolution HEIC decode is CPU-intensive per file, and the first version gave zero progress output and re-decoded every file on every run. **Fixed** in PR #13 (closed [#12](https://github.com/harveystephenson/London-2026/issues/12)): a filesystem-mtime pre-filter (source folders preserve original capture date as file mtime, so files nowhere near the trip window skip decode entirely) + manifest-reuse on re-run + progress logging brought a full run down to seconds/low-minutes even for hundreds of files.
- The date-window filter is intentionally content-blind — it grabs *everything* timestamped in the trip window (screenshots, unrelated shots, burst duplicates included), not just touristy photos. This is by design (a missed real trip photo is worse than a few extras to delete) — culling happens later in the manual review pass (#9, #10), not at extraction time. GPS-based filtering was considered but rejected since the user confirmed nearly all photos in the window are genuinely UK trip photos anyway (aside from the separate DC/Rania bucket).
- **Don't scrape claude.ai artifact content via browser automation by default.** The rendered artifact loads in a cross-origin iframe (`claudeusercontent.com`) that can't be navigated to directly; getting the raw source required awkward workarounds (chunked JS reads via the same-origin `/api/published_artifacts/...` endpoint). During one attempt, a silent file download was also triggered without asking the user first — that's a standing rule violation (downloads require explicit permission) and didn't even succeed (browsers block un-requested downloads). The user stopped this approach and prefers pasting content directly into chat. Prefer direct paste over scraping when the user can reasonably provide the content themselves, and always ask before triggering any file download regardless of source.
- This project was originally created from a Claude Code session rooted in a different repo (`macro_consensus`), which meant this file wasn't auto-loaded into context — it had to be fetched manually each time. Working from a session rooted directly in `London-2026` (rather than `cd`-ing into it from elsewhere) is the correct setup going forward so this file loads automatically.
- **The desktop app's local file preview panel does not reliably run the site's JS.** Removing a `fetch()` call (which is genuinely blocked under `file://`) didn't fix a blank gallery there — the user only got a working preview after running a real local HTTP server (`python -m http.server`) and opening that URL in an actual browser tab instead of the app's preview panel. Always verify changes via a real server, not just the preview panel; if something looks broken there but works over `http://localhost`, it's the panel, not the code.
- **iCloud Photos sync can silently get stuck for days**, even after manually unpausing and leaving it overnight, with zero visible progress in the Photos app. If this happens again: check icloud.com/photos directly to see if the phone→cloud upload even happened (separate from the Windows app's cloud→local download); check iCloud storage quota (Settings → [name] → iCloud → Manage Storage) since a full quota silently blocks new uploads; as a fallback, a direct USB import (see section 5) reliably works when iCloud doesn't.
- **`pkill -f "http.server ..."` from the Bash tool does not reliably kill background Python servers on this machine** — several accumulated as zombie processes across a session despite `pkill` reporting success. Use PowerShell instead to actually kill them: `Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -like "*http.server*" } | Stop-Process -Force`. (Confirmed again 2026-07-12: 10 zombies found holding port 8123; the PowerShell method cleared all of them.)
- **`.claude/launch.json` exists (added 2026-07-12)** with a `site` config (`python -m http.server 8123`) — use the Browser pane's `preview_start` with name `site` for local verification instead of ad-hoc background servers.
- **The in-app Browser pane's screenshots time out (~30s) on this machine even when the page is fine** — observed consistently on 2026-07-12 across localhost and external pages. `javascript_tool`, `get_page_text`, and `read_page` all work; verify pages via JS/DOM checks instead of screenshots, and treat screenshot timeouts as a pane quirk, not a page failure.
- **The in-app Browser pane is not signed into claude.ai** — artifact URLs render as a "Page not found / Sign in" page there. Verify published artifacts via WebFetch (which uses the authenticated session), and give the user the artifact link to open in their normal browser.
- **Browser image caching survives Ctrl+Shift+R for JS-loaded images** — hard refresh only bypasses cache for resources fetched during the page load itself; lightbox fulls (set via JS on click) can still serve stale cached bytes. Bit the user after the 2026-07-13 enhancement pass (enhanced thumbs beside original fulls, on desktop only; iPad + all servers verified correct by hashing at every hop). Fix shipped in PR #75: all dynamic photo URLs append `PHOTO_V = '?v=2'` — **bump it after any future mass re-encode** so every browser refetches once. Static servers ignore the query string.
- **`index.html`'s `<title>` contains a literal `&`, not `&amp;`** — exact-string transforms keyed on `&amp;` in the title will silently miss; match the tag with a regex instead (bit a variant-generation script on 2026-07-12).
- The Enter-key-inserts-newline issue the user hit is **not** a Claude Code keybindings problem — `~/.claude/keybindings.json` doesn't exist, so the default (`Enter` submits, `Ctrl+J` for newline) is in effect. If it recurs, it's likely a setting inside whatever chat surface the user is typing into (Desktop app preference), not this project or Claude Code's config.
- **This whole project folder lives inside OneDrive** (`...\OneDrive\Desktop\London-2026`). `build_gallery.py` writing hundreds of JPGs in one run has taken 15-20+ minutes here (vs. a low-minutes baseline elsewhere) — OneDrive's Files On-Demand sync client contends with the script for file I/O on every write. Since the #54 incremental rebuild landed, ordinary `build_gallery.py` runs write almost nothing and don't need any OneDrive precautions — the heads-up only still applies to genuinely large write runs: `build_gallery.py --force` (or after changing size/quality constants), or an `extract_trip_photos.py` run pulling in many new files. For those, proactively suggest pausing OneDrive syncing first (tray icon → gear → Pause syncing). User confirmed on 2026-07-12 they know where to find it, so just a heads-up is enough — no need to walk through it again.
- **OneDrive "large number of files permanently deleted" warnings (user saw these 2026-07-13 and 2026-07-14):** investigated and found no actual data loss — git history for tracked `photos/` showed no unexplained deletions, and the gitignored raw folders (`uk_trip`, `pruned_review`, etc.) were all intact. Most likely cause is legitimate churn from this project's own design: `build_gallery.py`'s automatic prune step (`existing_file.unlink()` on any thumb/full JPG no longer in the manifest — by design, these are regenerable derived images) combined with large-volume rewrite runs (e.g. the 1,434-file 2026-07-13 enhancement pass) hitting a OneDrive-synced folder. If it recurs and warrants more certainty, check onedrive.com's Recycle bin (or tray icon → "View online") for the actual flagged file list — Claude can't see OneDrive's own warning dialog or online recycle bin directly.
